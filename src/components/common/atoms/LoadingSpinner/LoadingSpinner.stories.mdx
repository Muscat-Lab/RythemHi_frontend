import { Meta, Canvas, Story } from '@storybook/blocks';

import LoadingSpinner from '.';

<Meta
  title="atoms/LoadingSpinner"
  component={LoadingSpinner}
/>

# LoadingSpinner

서비스에서 공통으로 사용되는 로딩스피너 컴포넌트 입니다.

# Props

```ts
interface LodingProps {
  //스피너의 크기! 'small', 'medium', 'large' 3가지 종류가 있습니다!
  loadingSize: SizeType;
}
```

### LoadingSpinner

<h4>* 로딩스피너 예시 *</h4>
<Canvas>
  <LoadingSpinner loadingSize="small" />
  <LoadingSpinner loadingSize="large" />
</Canvas>
