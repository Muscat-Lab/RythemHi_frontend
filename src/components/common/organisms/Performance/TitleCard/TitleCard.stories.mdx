import {
  Meta,
  Canvas,
  ArgsTable,
  Story,
} from '@storybook/blocks';

import TitleCard from '.';

<Meta title="organisms/TitleCard" component={TitleCard} />

# TitleCard

Performance 페이지 내에서 사용되어지는 맨 상단의 카드 형태의 컴포넌트.

## Props

```ts
interface TitleCardProps {
  // 카드 내 들어갈 이미지 url
  posterImageUrl: string;
}
```
