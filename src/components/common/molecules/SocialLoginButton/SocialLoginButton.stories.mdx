import { Meta, Canvas, Story } from '@storybook/blocks';

import SocialLoginButton from '.';

<Meta
  title="molecules/SocialLoginButton"
  component={SocialLoginButton}
/>

# SocialLoginButton

소셜 로그인 버튼 컴포넌트입니다.

# Props

```ts
interface ButtonProps {
  // 소셜 로그인 종류 'kakao', 'naver', 'apple'가 있습니다!
  variant?: VariantType;
  className?: string;
  // 입력할 텍스트
  text: string;
  // 필요한 아이콘
  iconKey?: SVGIconKeys;
}
```

### SocialLoginButton

<h4>* 소셜로그인버튼 예시 *</h4>

<div>
<SocialLoginButton iconKey="naver" variant="naver" text="네이버로 계속하기" />
<div style={{ marginTop: '10px' }}></div>
<SocialLoginButton iconKey="kakao" variant="kakao" text="카카오로 계속하기" />

</div>
